[project]
name = "mcpproject"
version = "0.1.0"
description = "My first time trying to build AI agents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.2.17",
    "langchain-mcp-adapters>=0.2.2",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.1.10",
    "mcp[cli]>=1.27.0",
]
